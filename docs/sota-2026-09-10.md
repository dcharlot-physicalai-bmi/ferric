# What to build next, ranked by what people actually download

**Method.** Every claim here is anchored to one of two things: a download count from the Hugging Face
API (top 120 GGUF repos, 141,987,186 downloads, pulled 2026-09-10) or an arch string read out of a
**real GGUF header** by range-requesting the first 8 KiB of the file. Nothing is ranked by
architecture count. Nothing is taken from a model card.

⚠ **Two false readings were caught by insisting on the header, and both would have changed the
ranking.** `gemma-4` first came back as `gemma4-assistant` — that file was the `MTP/` *draft* model,
while all three real gemma-4 repos say `gemma4`, which Ferric has. That error alone would have
invented a 10.2% gap. `Inkling` and `Qwen3.8-Flash-Next` first came back as `clip` — that was the
`mmproj` vision tower, not the text model. Read the neighbouring artifact, get a confident wrong
answer; the same shape as `ffn_out` vs `ffn_moe_out`.

## Coverage today: 93% of classified download volume runs

| arch (from real headers) | downloads | share | Ferric |
|---|---|---|---|
| `qwen35` | 53,618,630 | 37.8% | Verified |
| `gemma4` | 14,541,293 | 10.2% | Loads |
| `qwen3moe` | 14,471,700 | 10.2% | Loads |
| `qwen35moe` | 11,902,491 | 8.4% | Verified |
| `parakeet` | 6,719,127 | 4.7% | Verified |
| `laguna` / `bert` / `lfm2` / `llama` | 8,675,310 | 6.2% | Loads / Verified |
| **`deepseek4-dspark`** | 2,728,805 | 1.9% | ⛔ |
| **`qwen3vl`** | 2,138,286 | 1.5% | ⛔ |
| **`inkling`** | 1,413,473 | 1.0% | ⛔ |
| **`qwen4exp`** | 1,053,823 | 0.7% | ⛔ |
| **`chatglm`** | 995,713 | 0.7% | ⛔ |

⭐ **The Aug-2026 wave mostly REUSED arch strings Ferric already runs.** Qwen3.8-27B — roughly 29M
downloads across its variants, the single biggest thing on the list — reports
`general.architecture = qwen35`. So does Ornith (14M). KAT-Coder and POCKET-35B report `qwen35moe`.
"Support the latest models within 30 days" was already largely satisfied, and counting architectures
would never have shown it.

⚠ A real gap fails SAFELY: `arch::resolve` is an exact match returning `Unsupported`, so `qwen4exp`
is refused with a clear error rather than routed by `starts_with` into a wrong runtime — which
`qwen3.rs`'s `arch.starts_with("gemma")` could otherwise do.

## The techniques the corpus is actually shipping

Measured as download share of the same top-120:

| technique | share | Ferric |
|---|---|---|
| **MTP / multi-token prediction** | **7.5%** | qwen35 ONLY |
| **QAT checkpoints** | **5.6%** | format-transparent, runs |
| **NVFP4** (ggml type 40) | **1.7%** | ⛔ ABSENT |
| MXFP4 (type 39) | 0.0% by name | supported |

### 1. NVFP4 — the cheapest real win

`GGML_TYPE_NVFP4 = 40` merged into llama.cpp over Mar–Apr 2026 with CUDA, SYCL and Vulkan paths.

⛔ **THE GEOMETRY IN THE FIRST VERSION OF THIS DOC WAS WRONG.** It said "16 values per block, an FP8
scale per block plus one FP32 scale across the whole tensor", taken from a secondary write-up. The
struct in `ggml/src/ggml-common.h` says otherwise:

```c
#define QK_NVFP4 64
#define QK_NVFP4_SUB 16
typedef struct {
    uint8_t d[QK_NVFP4/QK_NVFP4_SUB]; // 4 UE4M3 scales, one per 16-value sub-block
    uint8_t qs[QK_NVFP4/2];           // 32 bytes of packed 4-bit E2M1
} block_nvfp4;                        // 36 B / 64 values = 4.5 bpw
```

So: **64 values per block, 36 bytes, four UE4M3 sub-block scales, and no per-tensor term inside the
block.** `block_bytes` therefore wants `40 => Some((64, 36))`.

⭐ **That makes it CHEAPER than the wrong spec implied, not harder.** `dequantize_row_nvfp4` looks
up **`kvalues_mxfp4`** — literally the same E2M1 table Ferric already ships as `KM` in
`MXFP4_HELPERS`. Only the scale is new.

⚠ Read the struct, not the blog. The write-up's spec would have produced a kernel with the wrong
stride and plausible-looking garbage.

### The verified spec, from ggml source (implement from THIS, not from prose)

Layout — `d[4]` then `qs[32]`, 36 B total. For sub-block `s` in 0..4, bytes `qs[s*8 + j]`, `j` in 0..8:

```text
    element s*16 + j      = kvalues_mxfp4[ qs[s*8+j] & 0x0F ] * ue4m3(d[s])
    element s*16 + j + 8  = kvalues_mxfp4[ qs[s*8+j] >>   4 ] * ue4m3(d[s])
```

Same low-half/high-half nibble split Ferric already uses for Q4_0, IQ4_NL and MXFP4 — but over a
**16-element sub-block**, not the whole block.

```c
static inline float ggml_ue4m3_to_fp32(uint8_t x) {
    if (x == 0 || x == 0x7F) return 0.0f;
    int exp = (x >> 3) & 0xF, man = x & 0x7;
    float raw = (exp == 0) ? ldexpf((float) man, -9)
                           : ldexpf(1.0f + (float) man / 8.0f, exp - 7);
    return raw * 0.5f;                      // ⭐ pairs with the DOUBLED kvalues table
}
```

⭐⭐ **THE `* 0.5f` IS THE WHOLE TRAP, AND FERRIC ALREADY KNOWS ITS SHAPE.** Quantisation stores
`ue4m3(amax / 6.0f)` where 6.0 is the max of the *undoubled* E2M1 table `{0,.5,1,1.5,2,3,4,6}`,
while dequant multiplies by the *doubled* `{0,1,2,3,4,6,8,12}`. The halving in the scale converter
is what reconciles them. Ship the table and the converter as a pair or the weights come out 2x.
This is the same class of hazard as MXFP4's `2^(e−128)` vs `2^(e−127)`, documented in
`MXFP4_HELPERS`.

✅ **Unlike E8M0, UE4M3 has NO subnormal hazard on this fabric.** The scale's range is `2^-10`
(exp=0, man=1) to 224 (0x7E) — every value comfortably normal in f32, so the split-exponent dance
`e8m0h` performs to survive flush-to-zero is unnecessary here.

**Verification plan** (the pattern already used for MXFP4): an exhaustive grid test over all 256
UE4M3 scale bytes x 16 E2M1 codes x both nibble halves, pinned to a CPU transliteration of the C
above — `mxfp4_kernel_matches_ggml_over_the_full_e8m0_x_e2m1_grid` is the template. ⚠ That CPU
reference and the kernel must not both be written from prose, or the test is circular; transliterate
the C, then cross-check a real checkpoint's first blocks by range-request.

Ferric's `QMatrix::block_bytes` has 39 (MXFP4, 32/17) and **no 40**. One repo in the top-120 already
ships it (`cdiamond/Qwen3.8-27B-iMatrix-NVFP4-MTP-GGUF`, 2.48M). This is a bounded, self-contained
addition: a block geometry, an unpack, and a matmul that already has five siblings to copy.

### 2. MTP is implemented but reaches one architecture

llama.cpp merged MTP in May 2026 (`--mtp`, off by default) and a community benchmark reported
Qwen3.6-27B going 38 → 65 tok/s on a 5090, **1.71x** at Q4_K_M.

Ferric already does this — `qwen35.rs` implements the `nextn` draft block
(`eh_proj(cat[enorm(embed(t+1)), hnorm(hidden)])`) and `ferric-serve` runs it. ⭐ **And Ferric is
AHEAD of the field in one specific way**: `specgate.rs` derives the acceptance rate at which
speculating is worth its energy — `a > E_d / E_m` — and refuses below it. No other runtime surveyed
prices speculation in joules; llama.cpp's flag is on/off.

⛔ **But `grep -ci mtp` across `crates/ferric-llama/src/` matches ONE file.** `gemma4` ships MTP
weights and is 10.2% of downloads.

⚠ **AND "generalise the loader" UNDERSTATES THE WORK — corrected after reading gemma4's MTP header.**
It is a different dialect, not the same block in another file:

| | qwen35 `nextn` | gemma4 `gemma4-assistant` |
|---|---|---|
| container | last layers of the SAME gguf | a SEPARATE file, its own arch string |
| depth | 1 draft block | **4** (`nextn_predict_layers = 4`) |
| width | main width | **1024**, against the main model's 3840 |
| projection | `eh_proj(cat[enorm(embed), hnorm(hidden)])` | `pre_projection [7680,1024]` -> blocks -> `post_projection [1024,3840]` |
| draft KV | its own cache | ⭐ **`attn_q` only — NO `attn_k`/`attn_v`**, so it reuses the main model's KV |
| per block | — | `layer_output_scale [1]`; `blk.3` is twice as wide as `blk.0..2` |

`7680 = 2 x 3840`, so `pre_projection` eats a concatenation of two main-width vectors — the same
*shape* of idea as qwen35's `eh_proj`, at a different width and depth, in a different container.
So a general MTP needs an abstraction over four axes (container, depth, projection, KV sharing), not
a loosened tensor-name match. Still ranked #1 on value; it is a port, not a refactor.

⭐ **This also un-retracts `gemma4-assistant`.** Above, it is called a false gap — correct for MAIN
MODEL coverage, since real gemma-4 checkpoints say `gemma4`. But it is a REAL arch that Ferric would
need registered to run gemma4 speculation. False gap for coverage, true gap for the draft path.

## The performance item, stated as one number

Same kernel, same fabric, quiet machine (load 3.84):

| | GB/s |
|---|---|
| one 176 MiB weight, streamed | **255.0** |
| the same total bytes as many small distinct weights | **38.9 – 68.0** |

A **3.7–6.6x penalty purely from splitting the stream into many small matmuls.** Whole-model Ferric
is 67 GB/s against llama.cpp's 156 on the same checkpoint — so whatever llama.cpp does differently,
it does it here, and the remaining 2.33x lives in this row and nowhere else.

⚠ Two things this is NOT, both checked rather than assumed:
- **Not host overhead.** `run_ids` reports host-side setup at 8.1% of wall (pipeline lookup 2.91 ms,
  bind groups 4.38, recording 0.30, submit 0.56, of 100.8 ms). 92% is GPU.
- **Not the submit floor.** The control measures a per-submit floor of ~60 us that is indifferent to
  bytes: `rmsnorm [1,4096]` reading 16 KiB costs 59.5 us, `matmul_q` reading 176 KiB costs 63.9 us —
  11x the data, 7% more time. The model issues 310 dispatches in 29 submits per token (10.7:1), so
  it pays that floor 29 times, not 310.

## The one place cross-fabric identity costs measurable speed

`MATMUL_Q4_K_SGGEMV_WGSL` exists — one subgroup per output, `subgroupAdd` instead of a shared-memory
barrier — and is deliberately opt-in behind `FERRIC_SGGEMV` because **subgroup reduction order is
hardware-dependent and would break bit-identity across adapters**. Same for coop_matrix. llama.cpp
makes no identity promise and uses simdgroup reductions freely.

⚠ **The exchange rate is currently UNKNOWN and should not be guessed.** The kernel exists only for
Q4_K, reuses the FLAT weight buffers, and `matmul_q` does not route the FFN (that goes through
`try_matmul_swiglu`), so measuring it today would price a fraction of the graph. Pricing it honestly
needs a Q5_K subgroup kernel and a subgroup SwiGLU variant. Worth doing deliberately: if determinism
costs 1.5x, that belongs in the README as a documented flag, not discovered by a user.

## Where the browser lever is

LlamaWeb (arXiv 2605.20706) reports up to **69% throughput gain** on WebGPU and attributes its weak
prefill to *the absence of kernel fusion and subgroup matrix ops in the browser path*. Ferric has the
fusion (`qk_norm_rope`, `matmul_q*_swiglu`, `add_rmsnorm`). WebGPU **subgroup matrix** is not in
stable browsers yet; W3C discussion is ongoing. Ferric's `coop_matrix` is native-gated (Vulkan +
Metal 4 MPP), so the browser path inherits it the day the feature stabilises — provided the kernel is
written against the WGSL type and not against MPP directly.

## Ranked

1. **MTP past `qwen35`** — 7.5% of downloads ship MTP weights; 1.71x where it applies; the energy
   gate is already built and is a genuine differentiator. ⚠ Costed as a PORT (four axes differ), not
   the refactor this line first claimed.
2. **NVFP4 (type 40)** — bounded work, already in the corpus, and the two-level scale makes a naive
   MXFP4 port silently wrong.
3. **The split-stream penalty** — the entire remaining 2.33x, isolated to one measurable row.
4. **Arch ports in download order** — `deepseek4-dspark`, `qwen3vl`, `inkling`, `qwen4exp`, `chatglm`.
5. **Price the determinism trade** — needs a Q5_K subgroup kernel first; do not quote a number before.

## On "open weights will win because software will win"

The download data is consistent with the thesis and sharpens it. 141,987,186 downloads across 120
repos, and the top of the list is not a frontier lab's API — it is `unsloth`, `ornith-ai`,
`lmstudio-community`, `bartowski`, `mudler`, and a long tail of community requantisations of the same
few base models. **The base weights are a commodity; the packaging is where the volume is.** Eleven
of the top 120 ship MTP draft heads and 5.6% are QAT checkpoints — the corpus is now shipping
*runtime-facing artifacts*, not just weights, which is precisely the surface a runtime competes on.

⭐ Ferric's defensible position is not tok/s, which llama.cpp will keep contesting. It is the two
things nothing else on this list has: **one graph that is bit-identical across fabrics**, and **an
energy model that can refuse work that does not pay for itself**. Both are software, and both get
harder to copy the longer they run.

## 2026-09-14 addendum — the deletion test resolved the OPPOSITE way, and the NVIDIA tier is real

⛔ **CORRECTED LABEL.** The first version of this section said "RTX 4050 (Vulkan via wgpu)". It was
the **Intel Iris Xe (RPL-P), Mesa ANV, via wgpu/Vulkan** — the box's NVIDIA Vulkan ICD fails
`vkCreateInstance` (`ERROR_INCOMPATIBLE_DRIVER`, userspace 595.84 vs kernel module 595.71.05) and
wgpu's `HighPerformance` preference silently took the iGPU. Nothing printed the adapter; now
`run_ids`/`kv_bitref`/`ab_env.sh` do, first line, ungated (vacuous-test-mechanisms #71).
**Ferric on NVIDIA via wgpu is NOT YET MEASURED.** The native CUDA tier (libcuda) is the only Ferric
path that reached the 4050 that day, and it is correctness-only.

Measured on the Intel Iris Xe (Vulkan via wgpu), uncontended (load 0.2), `scripts/quiet.sh` +
`scripts/ab_env.sh`, generation identical across arms, logits fingerprints differ:

| lane layout | ms/tok min | spread | paired | verdict |
|---|---|---|---|---|
| default | 101.6 | 1.4 | — | — |
| **two-level sub-block (`FERRIC_SUBBLK=1`)** | **91.5** | 0.4 | **7/7, p=0.016** | **RESOLVED, +11%** — re-run on a verified file: 101.2 → 90.8, 4/4 |
| `wide` (64 lanes, `opw=1`) | 121.6 | 22.8 | 0/7 | **20% slower** — "idle lanes are free" is false here |

⛔ The prediction was backwards: the 3-line `wide` was expected to make the 60-line sub-block split
deletable. It is the other way round. On the M5 Max (Metal) the same sub-block split was
**INDISTINGUISHABLE** (p=0.125), so the right lane layout is **fabric-dependent** — the
"dedicated code per hardware stack" thesis as a measurement. Design consequence: the PORTABLE tier
keeps ONE layout everywhere (that is what cross-fabric bit-identity means); per-fabric layouts are
opt-in performance profiles with their own recorded fingerprint. **Recommendation on Vulkan/NVIDIA:
`FERRIC_SUBBLK=1`. Never `wide`.**

**NVIDIA native tier (driver API over dlopen, no wrapper crate, no CUTLASS/cuBLAS):** Q5_K GEMV in
CUDA C shipped as `cuda_q5k_gemv.cu` + prebuilt `.ptx` (compute_75), verified on the RTX 4050 against
the WGSL FLAT kernel through an explicit seam: **max |Δ| = 4.578e-4 on 5.144e2** (rel 8.9e-7), hook
check bit-exact, PTX byte-reproducible from source. ⛔ The first run "passed" with Δ = 0.000e0 — the
reference had been routed to CUDA by the new hook (vacuous-test-mechanisms #68). ⚠ Tier 1 copies
activations in/out per matmul via a wgpu readback, so it is a CORRECTNESS tier: its end-to-end speed
is not a native-tier number and has deliberately not been quoted. Tier 2 = whole graph resident.

**The NVIDIA parity number, both sides on the real RTX 4050 (sm_89), same Q5_K_M GGUF:**

| | ms/tok | tok/s | how |
|---|---|---|---|
| llama.cpp 97e4ca7, CUDA backend | **4.46** | 224.02 ± 1.23 | `llama-bench -ngl 99`, tg128 |
| **Ferric native tier 2** (resident CUDA decode) | **9.3** (min of 7, load-gated, shipping tree) | 107.5 | `run_ids`, 24 tokens, per-token H2D row + D2H logits included |
| ratio | **2.09x** | | the same band as Metal's 2.33x |

Ferric's number is the tier-2 path: the whole dense decode step resident on the device — seven
kernels in CUDA C (no cuBLAS/CUTLASS), driver API over dlopen, shipped PTX — after a WGSL prefill
whose K/V is copied in once. Correctness: **generated ids identical to the WGSL path over 16 tokens
through all 28 layers**, logits fingerprint moved, and every kernel checked on the device against its
WGSL twin through a hermetic seam — Q6_K GEMV vs FLAT (Δ 1.831e-3 on 1.758e3), fused SwiGLU vs
composed (Δ 1.562e-1 on 1.371e5), rmsnorm (plus an independent f64 CPU reference) / QK-norm-RoPE /
attention vs their `Tensor` methods — 4/4 serialized.

⛔ Three things this surfaced, all fixed or isolated before this paragraph was written:
1. The first tier-2 commit CLAIMED the per-kernel tests and did not contain them (a patch script
   aborted before writing). Caught by "88 filtered out" being the pre-tier-2 count.
2. The CUDA context was bound on the opening thread only. Each test passed ALONE, all failed
   serialized on fresh threads. A driver context is per-thread; every entry point now re-binds — a
   bug `ferric-serve`'s request threads would have hit.
3. In PARALLEL the test process still segfaults — and the backtrace is `0x0` inside `libvulkan.so.1`
   under `wgpu::Instance::new`, and the plain non-CUDA GPU tests crash the same way with `FERRIC_CUDA`
   unset. That is this box's broken NVIDIA Vulkan ICD racing concurrent instance creation into a null
   call; not the native tier. The parallel gate cannot pass there until the box reboots.

⚠ Ferric's figure carries a per-token `cuCtxSynchronize` and a 600 KB logits D2H that llama-bench's
steady-state loop does not; and the box's wgpu path still cannot see the 4050 (Vulkan ICD mismatch
until reboot), so "Ferric-WGSL on NVIDIA" remains unmeasured. A "20x" figure was briefly computed
against iGPU numbers earlier that day; it is void and was never published.

From "no Ferric-on-NVIDIA number exists" to 2.09x in one day of tier-2 work: the portable graph is the
floor everywhere, the dedicated path is the ceiling per fabric, and one harness gates both.
