# Ferric-owned fork of `naga`
Promoted out of `vendor/` to a first-class fork wired via `[patch.crates-io]`. We maintain and evolve
it in-ecosystem (custom kernel codegen / fusion for Ferric's L1). Upstream-tracked, not upstream-dependent.
